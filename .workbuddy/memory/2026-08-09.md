# 2026-08-09 工作日志

## 消防备考知识库 → 手机 AI 问答网站（已完成并部署）

**背景**：用户想把"一级消防工程师备考问答"做成手机可访问、可分享给他人的形态。

**成果**：
- 在项目外 `C:\Users\1\.workbuddy\fire-kb-site\` 构建了纯静态 SPA 网站（遵守 AGENTS.md 只读边界，未写入项目内任何知识库位置）
- 内容：知识库 41 条目 + 奇门口诀 10 组 + 讲义 OCR 6 本（1880 页 → 552 片段）全文检索
- 功能：首页模块浏览、全局搜索（中文 bigram 倒排索引 tf*idf）、条目详情、口诀分组、AI 问答（RAG：本地检索 top-K 拼 prompt → 浏览器直连 OpenAI 兼容接口流式输出，答案要求附规范条文）
- 部署：CloudStudio 公网链接 `https://3d38f61b3c304b2b9e4ac52a98306b86.gz3.agentos-app.net`（设置-数据管理-我发布的应用 可管理）
- AI 接口：用户自备 OpenAI 兼容 base URL + Key + 模型名，网页 ⚙ 设置面板存入 localStorage（不内嵌，他人各自配置）

**技术要点**：
- 构建：`fire-kb-site/build/build.mjs`（gray-matter + marked + 自研 bigram 倒排索引），依赖装在项目外
- 关键 bug 修复：OCR 分页时 `cur.text` 在 push 后才赋值导致 0 页；模板字符串闭合错误导致 app.js 语法错误
- OCR 索引文件用 ASCII 编号（ocr-1..6.json）避免中文文件名在静态托管/移动端兼容问题
- Windows safe-delete 会拦截中文路径删除（bash rm / node unlinkSync 均失败），需 mv 改名或重建目录绕过

**待办**：
- 用户提供 base URL/Key/模型名后验证 CORS；若网关禁 CORS 需换支持 CORS 的网关
- 旧构建备份 `fire-kb-site/dist-old-0823/` 可手动删除（含中文名旧文件，safe-delete 无法自动删）

## 问答优先级修复（23:24）

**问题**：用户反馈网页 AI 回答不优先引用知识库/讲义，直接给规范条文。

**原因**：① system prompt 未规定"知识库 > 讲义 > 规范"优先级层级；② 检索兜底文案"请基于你的专业知识谨慎回答"鼓励模型自由发挥。

**修复**（`fire-kb-site/site/assets/app.js`）：
- system prompt 明确四级规则：知识库条目（最高优先）→ 讲义 OCR → 规范条文（须先明说未检索到）→ 资料不足直接答"未收录"，禁止编造
- 兜底文案改为"知识库与讲义均未检索到相关内容"
- context 中 KB 片段加"[知识库·最高优先级·…]"前缀
- 已重新构建并部署（同沙箱，链接不变），线上已验证包含修复内容
- 提醒用户：浏览器可能缓存旧 JS，需强制刷新
